{
  "name": "pelada-manager-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend \"npm --workspace backend run dev\" \"npm --workspace frontend run dev\"",
    "build": "npm --workspace backend run build && npm --workspace frontend run build",
    "start": "npm --workspace backend run start",
    "deploy:ghpages": "npm --workspace frontend run deploy"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
