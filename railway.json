{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "watchPatterns": [
      "backend/**",
      "frontend/**"
    ]
  },
  "deploy": {
    "startCommand": "cd backend && npm run start"
  }
}
